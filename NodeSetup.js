const State = require('./State');

class NodeSetup {
    constructor() {
        this.backup = null;
    }

    convertJSONToInstance(nodeData) {
        console.log(nodeData);
    }
}

module.exports = NodeSetup;